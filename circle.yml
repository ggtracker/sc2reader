general:
  branches:
    ignore:
      - ggmaster
      - hots
      - jonomon
      - master
      - old_master
test:
    override:
        - python -m unittest discover test_replays
        - python -m unittest discover test_s2gs
